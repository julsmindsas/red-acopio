# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# Red de Acopio
# Store JSON local mutable (desarrollo). La semilla versionada es data/centers.seed.json
data/centers.local.json
# Hogares de paso: contiene nombres, teléfonos, documentos y direcciones de
# personas reales. JAMÁS debe entrar al repositorio.
data/hogares.local.json
# Salida cruda del scraper (datos sin curar). Lo curado se integra a la semilla.
/data/scraped/

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.env*

# Skills de agentes instaladas por integraciones (Resend). Las reinstala
# el propio 'vercel integration add'; no son codigo del proyecto.
.agents/
.claude/skills/
skills-lock.json
